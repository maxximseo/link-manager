# Быстрый импорт для Claude Code

## Что делать когда пользователь пришлет export файл

### Вариант 1: Пользователь прислал JSON содержимое

```bash
# Создать файл с timestamp
TIMESTAMP=$(date +%s)
# Сохранить содержимое в migration-data/export-$TIMESTAMP.json

# Запустить импорт
node scripts/import-to-supabase.js migration-data/export-$TIMESTAMP.json
```

### Вариант 2: Пользователь прислал путь к файлу

```bash
# Напрямую импортировать
node scripts/import-to-supabase.js [путь-к-файлу]
```

### Вариант 3: Файл уже в migration-data/

```bash
# Найти последний export файл
ls -lt migration-data/export-*.json | head -1

# Запустить импорт
node scripts/import-to-supabase.js migration-data/export-XXXXXX.json
```

## Проверка после импорта

```bash
# 1. Базовая проверка подключения и статистики
node test-supabase-connection.js

# 2. Запустить приложение
npm run dev

# 3. Проверить что работает:
# - Авторизация (логин/регистрация)
# - Список проектов
# - Список сайтов
# - Создание placement
```

## Ожидаемый вывод при успешном импорте

```
========================================
ИМПОРТ ДАННЫХ В SUPABASE
========================================

📦 Читаем файл экспорта...
✅ Файл загружен: 15 таблиц, 1234 записей

🔌 Подключение к Supabase...
✅ Подключено к: nuykefcuninjboyrcxcn.supabase.co

📝 Импорт данных...
✅ users: 10 записей импортировано
✅ projects: 5 записей импортировано
✅ sites: 100 записей импортировано
✅ project_links: 25 записей импортировано
✅ project_articles: 3 записей импортировано
✅ transactions: 50 записей импортировано
✅ placements: 30 записей импортировано
✅ placement_content: 30 записей импортировано
⚠️  discount_tiers: 10 записей пропущено (уже существуют)
✅ renewal_history: 5 записей импортировано
✅ referral_transactions: 2 записей импортировано
✅ referral_withdrawals: 1 записей импортировано
✅ notifications: 15 записей импортировано
✅ audit_log: 100 записей импортировано
✅ registration_tokens: 3 записей импортировано

🔄 Обновление sequences...
✅ Все sequences обновлены

========================================
✅ ИМПОРТ ЗАВЕРШЕН УСПЕШНО
========================================

Статистика:
- Всего записей: 1234
- Импортировано: 1224
- Пропущено: 10 (дубликаты)
- Ошибок: 0

Время выполнения: 2.5 секунд
```

## Если что-то пошло не так

### Ошибка подключения
```bash
# Проверить .env
cat .env | grep DB_

# Проверить что Supabase доступен
node -e "const { Pool } = require('pg'); const pool = new Pool({ host: process.env.DB_HOST, port: process.env.DB_PORT, database: process.env.DB_NAME, user: process.env.DB_USER, password: process.env.DB_PASSWORD, ssl: { rejectUnauthorized: false } }); pool.query('SELECT NOW()').then(r => console.log('OK:', r.rows[0])).catch(e => console.error('ERROR:', e.message)).finally(() => pool.end());"
```

### Ошибка в данных
```bash
# Скрипт автоматически откатит транзакцию
# Проверить логи для деталей
# Исправить данные в export файле
# Повторить импорт
```

### Дубликаты
```bash
# Это нормально! Скрипт пропускает дубликаты
# Если нужно перезаписать данные:
# 1. Удалить старые данные из таблиц
# 2. Запустить импорт заново
```

## Быстрая команда "все в одном"

```bash
# Если файл уже есть в migration-data/
EXPORT_FILE=$(ls -t migration-data/export-*.json 2>/dev/null | head -1)
if [ -f "$EXPORT_FILE" ]; then
  echo "Найден файл: $EXPORT_FILE"
  node scripts/import-to-supabase.js "$EXPORT_FILE" && \
  echo "✅ Импорт завершен!" && \
  echo "Проверяем подключение..." && \
  node test-supabase-connection.js
else
  echo "❌ Export файл не найден в migration-data/"
  echo "Попросите пользователя сделать экспорт или прислать файл"
fi
```

---

**Готов!** Скопируйте эти команды и используйте когда пользователь пришлет данные.
